/**
 * btf-lib-v1 — stub index
 *
 * This is a placeholder module. In production, replace with:
 *   npm link btf-lib-v1          (local dev)
 *   git submodule add <url>      (deployment)
 *
 * All sub-modules (secret/*, logging/*) are importable individually.
 */

module.exports = {
  version: "0.0.1-stub",
  isStub: true,
};
