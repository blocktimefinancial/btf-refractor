/**
 * azureIntegration stub — btf-lib-v1/logging/azureIntegration
 *
 * Azure Application Insights integration for logging.
 * Stub until real library is linked.
 */

module.exports = {
  createAzureTransport: () => null,
  initializeAppInsights: () => {},
};
